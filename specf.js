
const AbsolNumber = alert ("Write a javascript program to compute the absolute difference between specified number and 19 Returns triple their absolute difference If the specified number is greater than 19");


function SpecNumber (p) { 
    

    if (p > 19) {
        
        return 3 * (p - 19);
    }

    else {
        
        return (p - 19);

    }
    
}

document.write  (SpecNumber (22) );
